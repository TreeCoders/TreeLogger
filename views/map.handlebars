<div class="container-fluid" id="mapContainer">
        <div class="row" id="mapBtnRow" data-aos="fade-right">
            <div class="col-md-12" id="mapFloatingPanel">
            </div>
        </div>
        <div class="row" id="mapRow" data-aos="fade-left">
            <div class="col-md-12" id="mapCol">
                <div id="map">
                    <h3>My Google Maps Demo</h3>
    <!--The div element for the map -->
    <div id="map"></div>
    <script>
// Initialize and add the map
function initMap() {
  // The location of Uluru
  var uluru = {lat: 37.7913908, lng: -122.3948302};
  // The map, centered at Uluru
  var map = new google.maps.Map(
      document.getElementById('map'), {zoom: 15, center: uluru});
  // The marker, positioned at Uluru
  var marker = new google.maps.Marker({position: uluru, map: map});
}
    </script>
    <script async defer
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAny8QhQ3ISkkGwCimkBoNfskamSbb7hBU&callback=initMap">
    </script>
                </div>
            </div>
        </div>
    </div>